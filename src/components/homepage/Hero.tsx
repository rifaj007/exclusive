import HeroNav from "./HeroNav";
import HeroSlider from "./HeroSlider";

const Hero = () => {

  return (
    <section className="mb-[140px] flex">
      <HeroNav/>
      <HeroSlider/>
    </section>
  );
};

export default Hero;
